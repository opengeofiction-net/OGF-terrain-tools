[Unit]
Description=Parse the API access log into changeset and IP data

[Timer]
# hourly at :34, as the old ogf crontab
OnCalendar=*:34
Persistent=true
Unit=ogfutil-parseAccessLog.service

[Install]
WantedBy=timers.target
